#ifndef MATRIX_OPERATION_H
#define MATRIX_OPERATION_H
#include "Matrix.h"

class MatrixOperation 
{
    public:
    Matrix sumOfMatrix(const Matrix &matrix1, const Matrix &matrix2);
    Matrix productOfMatrix(const Matrix &matrix1, const Matrix &matrix2);
};

#endif
